/**
 * Class names for links and link-style buttons inside running text: on touch screens
 * (coarse pointers) they become at least 44px tall and wide, as every other control
 * there; with a mouse they stay plain inline text.
 */
export const TOUCH_INLINE =
  'pointer-coarse:inline-flex pointer-coarse:min-h-11 pointer-coarse:min-w-11 pointer-coarse:items-center pointer-coarse:justify-center';
